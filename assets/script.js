const start = document.getElementById("startButton");

start.addEventListener("click", startGame) {
    function startGame() {
        if (i=75; i > 0; i--;) {
            timerLeft = i + " second(s) remaining!";
        }

    }        

}