const start = document.getElementById("startButton");
let timerLeft = 75;

start.addEventListener("click", startGame) {
    function startGame() {
        if (timerLeft; timerLeft > 0; timerLeft--;) {
            timerLeft = timerLeft + " second(s) remaining!";
        }

    }        

}